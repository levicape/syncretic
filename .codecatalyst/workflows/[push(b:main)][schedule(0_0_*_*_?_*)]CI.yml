########################################
# THIS FILE WAS AUTOMATICALLY GENERATED, DO NOT MODIFY
########################################
Name: main_OnPush__CI_CD
RunMode: SUPERSEDED
SchemaVersion: "1.0"
Compute:
  Type: EC2
  Fleet: Linux.Arm64.XLarge
Triggers:
  - Type: PUSH
    Branches:
      - main
  - Type: SCHEDULE
    Expression: 0 0 * * ? *
    Branches:
      - main
Actions:
  Integration:
    Actions:
      Install:
        Identifier: aws/build@v1.0.0
        Timeout: 19
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
            a64_docker:
              Path: /tmp/docker-cache
              RestoreKeys:
                - docker
            a64_pulumi:
              Path: /tmp/pulumi
              RestoreKeys:
                - pulumi
            a64_python:
              Path: /root/.pyenv
              RestoreKeys:
                - python
        Inputs:
          Sources:
            - WorkflowSource
          Variables:
            - Name: NODEJS_VERSION
              Value: "23"
            - Name: NPM_REGISTRY_PROTOCOL
              Value: https
            - Name: NPM_REGISTRY_HOST
              Value: npm.pkg.github.com
            - Name: NODE_AUTH_TOKEN
              Value: ${Secrets.GITHUB_LEVICAPE_PAT}
            - Name: PAKETO_CLI_IMAGE
              Value: buildpacksio/pack:latest
            - Name: PAKETO_BUILDER_IMAGE
              Value: heroku/builder:24
            - Name: PAKETO_LAUNCHER_IMAGE
              Value: heroku/heroku:24
            - Name: PULUMI_VERSION
              Value: 3.147.0
            - Name: PYTHON_VERSION
              Value: 3.11.6
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: npm config set @levicape:registry=$NPM_REGISTRY_PROTOCOL://$NPM_REGISTRY_HOST --location project
            - Run: npm config set //$NPM_REGISTRY_HOST/:_authToken=$NODE_AUTH_TOKEN --location project
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm root -g
            - Run: npm install --g pnpm
            - Run: npm install --g n
            - Run: npm exec n $NODEJS_VERSION
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec pnpm install --ignore-scripts
            - Run: mkdir -p /tmp/npm-global
            - Run: mkdir -p /tmp/pnpm-store
            - Run: mkdir -p /tmp/docker-cache
            - Run: mkdir -p /tmp/pulumi
            - Run: mkdir -p /tmp/docker-cache/images
            - Run: docker load --input /tmp/docker-cache/images/cli-pack.tar.gz || true
            - Run: docker pull $PAKETO_CLI_IMAGE
            - Run: docker save $PAKETO_CLI_IMAGE | gzip > /tmp/docker-cache/images/cli-pack.tar.gz
            - Run: du -sh /tmp/docker-cache/images/cli-pack.tar.gz
            - Run: docker load --input /tmp/docker-cache/images/builder.tar.gz || true
            - Run: docker pull $PAKETO_BUILDER_IMAGE
            - Run: docker save $PAKETO_BUILDER_IMAGE | gzip > /tmp/docker-cache/images/builder.tar.gz
            - Run: du -sh /tmp/docker-cache/images/builder.tar.gz
            - Run: docker load --input /tmp/docker-cache/images/launcher.tar.gz || true
            - Run: docker pull $PAKETO_LAUNCHER_IMAGE
            - Run: docker save $PAKETO_LAUNCHER_IMAGE | gzip > /tmp/docker-cache/images/launcher.tar.gz
            - Run: du -sh /tmp/docker-cache/images/launcher.tar.gz
            - Run: "[ -f /tmp/pulumi/bin/pulumi ] && /tmp/pulumi/bin/pulumi version | grep $PULUMI_VERSION || curl -fsSL https://get.pulumi.com | sh -s -- --version $PULUMI_VERSION --install-root /tmp/pulumi"
            - Run: du -sh /tmp/pulumi
            - Run: npm exec pnpm list
            - Run: du -sh node_modules /tmp/npm-global /tmp/pnpm-store
            - Run: which pyenv
            - Run: eval "$(pyenv init -)"
            - Run: git clone https://github.com/pyenv/pyenv-update.git $(pyenv root)/plugins/pyenv-update
            - Run: pyenv update || true
            - Run: pyenv install $PYTHON_VERSION || true
            - Run: pyenv global $PYTHON_VERSION || true
            - Run: pyenv versions || true
            - Run: python3 -m pip install -r requirements.txt
      Compile:
        Identifier: aws/build@v1.0.0
        DependsOn:
          - Install
        Timeout: 19
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
            a64_python:
              Path: /root/.pyenv
              RestoreKeys:
                - python
        Inputs:
          Sources:
            - WorkflowSource
          Variables:
            - Name: NODEJS_VERSION
              Value: "23"
            - Name: NPM_REGISTRY_PROTOCOL
              Value: https
            - Name: NPM_REGISTRY_HOST
              Value: npm.pkg.github.com
            - Name: NODE_AUTH_TOKEN
              Value: ${Secrets.GITHUB_LEVICAPE_PAT}
            - Name: PYTHON
              Value: /root/.pyenv/shims/python3
        Outputs:
          AutoDiscoverReports:
            Enabled: true
            ReportNamePrefix: junit
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: npm config set @levicape:registry=$NPM_REGISTRY_PROTOCOL://$NPM_REGISTRY_HOST --location project
            - Run: npm config set //$NPM_REGISTRY_HOST/:_authToken=$NODE_AUTH_TOKEN --location project
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm exec n $NODEJS_VERSION
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec pnpm install --prefer-offline --ignore-scripts
            - Run: sudo yum install -y g++ make cmake zip unzip libcurl-devel automake autoconf libtool zlib zlib-devel zlib-static
            - Run: sudo yum install -y jq || true
            - Run: sudo yum install -y protobuf protobuf-devel protobuf-compiler || true
            - Run: sudo yum install -y sqlite sqlite-devel sqlite-libs sqlite-tools || true
            - Run: python3 -c "print('ok')" || true
            - Run: npm rebuild node-gyp
            - Run: npm rebuild knex better-sqlite3
            - Run: npm exec pnpm rebuild || true
            - Run: npm exec pnpm build
            - Run: npm exec pnpm lint
            - Run: npm exec pnpm test
            - Run: du -sh $(pwd)/**/module $(pwd)/**/commonjs || true
      Image:
        Identifier: aws/build@v1.0.0
        DependsOn:
          - Install
        Timeout: 19
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
            a64_docker:
              Path: /tmp/docker-cache
              RestoreKeys:
                - docker
            a64_python:
              Path: /root/.pyenv
              RestoreKeys:
                - python
        Inputs:
          Sources:
            - WorkflowSource
          Variables:
            - Name: NODEJS_VERSION
              Value: "23"
            - Name: NPM_REGISTRY_PROTOCOL
              Value: https
            - Name: NPM_REGISTRY_HOST
              Value: npm.pkg.github.com
            - Name: NODE_AUTH_TOKEN
              Value: ${Secrets.GITHUB_LEVICAPE_PAT}
            - Name: APPLICATION_IMAGE_NAME
              Value: fourtwo
            - Name: PYTHON
              Value: /root/.pyenv/shims/python3
        Outputs:
          Artifacts:
            - Name: images
              Files:
                - _images/**/*
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: docker load --input /tmp/docker-cache/images/cli-pack.tar.gz || true
            - Run: docker load --input /tmp/docker-cache/images/builder.tar.gz || true
            - Run: docker load --input /tmp/docker-cache/images/launcher.tar.gz || true
            - Run: mkdir -p _images
            - Run: npm config set @levicape:registry=$NPM_REGISTRY_PROTOCOL://$NPM_REGISTRY_HOST --location project
            - Run: npm config set //$NPM_REGISTRY_HOST/:_authToken=$NODE_AUTH_TOKEN --location project
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm exec n $NODEJS_VERSION
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec pnpm install --prefer-offline --ignore-scripts
            - Run: sudo yum install -y g++ make cmake zip unzip libcurl-devel automake autoconf libtool zlib zlib-devel zlib-static
            - Run: sudo yum install -y jq || true
            - Run: sudo yum install -y protobuf protobuf-devel protobuf-compiler || true
            - Run: sudo yum install -y sqlite sqlite-devel sqlite-libs sqlite-tools || true
            - Run: npm rebuild node-gyp
            - Run: npm rebuild knex better-sqlite3
            - Run: npm exec pnpm rebuild || true
            - Run: npm exec pnpm exec nx pack:build iac-images-application --verbose
            - Run: docker save $APPLICATION_IMAGE_NAME | gzip > _images/application.tar.gz
            - Run: du -sh _images/application.tar.gz
            - Run: ls -la _images
      Current:
        Identifier: aws/build@v1.0.0
        DependsOn:
          - Image
        Timeout: 19
        Environment:
          Name: current
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
            a64_pulumi:
              Path: /tmp/pulumi
              RestoreKeys:
                - pulumi
        Inputs:
          Sources:
            - WorkflowSource
          Variables:
            - Name: NODEJS_VERSION
              Value: "23"
            - Name: NPM_REGISTRY_PROTOCOL
              Value: https
            - Name: NPM_REGISTRY_HOST
              Value: npm.pkg.github.com
            - Name: NODE_AUTH_TOKEN
              Value: ${Secrets.GITHUB_LEVICAPE_PAT}
            - Name: APPLICATION_IMAGE_NAME
              Value: fourtwo
            - Name: CI_ENVIRONMENT
              Value: current
            - Name: AWS_REGION
              Value: us-west-2
            - Name: FRONTEND_HOSTNAME
              Value: fourtwo.levicape.cloud
            - Name: PULUMI_HOME
              Value: /tmp/pulumi
            - Name: PULUMI_CONFIG_PASSPHRASE
              Value: ${{ Secrets.PULUMI_CONFIG_PASSPHRASE }}
          Artifacts:
            - images
        Outputs:
          Artifacts:
            - Name: pulumi
              Files:
                - _pulumi/**/*
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: npm config set @levicape:registry=$NPM_REGISTRY_PROTOCOL://$NPM_REGISTRY_HOST --location project
            - Run: npm config set //$NPM_REGISTRY_HOST/:_authToken=$NODE_AUTH_TOKEN --location project
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec n $NODEJS_VERSION
            - Run: npm exec pnpm install --prefer-offline --ignore-scripts
            - Run: npm exec pnpm build
            - Run: ls -la $CATALYST_SOURCE_DIR_images/_images
            - Run: docker load --input $CATALYST_SOURCE_DIR_images/_images/application.tar.gz
            - Run: docker images
            - Run: aws ssm get-parameter --name /fourtwo/_principal/pulumi/backend/StateBackendCommandReference
            - Run: docker run --rm -e CI=true -e AWS_EXECUTION_ENV -e AWS_CONTAINER_CREDENTIALS_FULL_URI --network="host" --entrypoint launcher fourtwo -- pnpm run dx:cli:mjs aws pulumi ci --region $AWS_REGION > .pulumi-ci
            - Run: cat .pulumi-ci
            - Run: cat .pulumi-ci | grep "export" >> .export-cd
            - Run: cat .export-cd
            - Run: source .export-cd
            - Run: mkdir _pulumi
            - Run: /tmp/pulumi/bin/pulumi stack init "$APPLICATION_IMAGE_NAME-codestar.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/codestar || true
            - Run: /tmp/pulumi/bin/pulumi stack select "$APPLICATION_IMAGE_NAME-codestar.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/codestar || true
            - Run: /tmp/pulumi/bin/pulumi config set aws:skipMetadataApiCheck false -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.isProd false -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.features aws -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi config set --path 'frontend:stack.dns.hostnames[0]' "$CI_ENVIRONMENT.$APPLICATION_IMAGE_NAME.cloud.$FRONTEND_HOSTNAME" -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi stack change-secrets-provider $AWS_PROVIDER_KEY -C $(pwd)/iac/stacks/src/codestar
            - Run: /tmp/pulumi/bin/pulumi refresh -C $(pwd)/iac/stacks/src/codestar --yes --skip-preview --clear-pending-creates --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-refresh"
            - Run: /tmp/pulumi/bin/pulumi preview -C $(pwd)/iac/stacks/src/codestar  --show-replacement-steps --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-preview"
            - Run: /tmp/pulumi/bin/pulumi up -C $(pwd)/iac/stacks/src/codestar --yes --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-up"
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/codestar --json > $(pwd)/_pulumi/codestar.json
            - Run: cat _pulumi/codestar.json
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/codestar --shell > $(pwd)/_pulumi/codestar.sh
            - Run: cat _pulumi/codestar.sh
            - Run: /tmp/pulumi/bin/pulumi stack init "$APPLICATION_IMAGE_NAME-datalayer.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/datalayer || true
            - Run: /tmp/pulumi/bin/pulumi stack select "$APPLICATION_IMAGE_NAME-datalayer.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/datalayer || true
            - Run: /tmp/pulumi/bin/pulumi config set aws:skipMetadataApiCheck false -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.isProd false -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.features aws -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi config set --path 'frontend:stack.dns.hostnames[0]' "$CI_ENVIRONMENT.$APPLICATION_IMAGE_NAME.cloud.$FRONTEND_HOSTNAME" -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi stack change-secrets-provider $AWS_PROVIDER_KEY -C $(pwd)/iac/stacks/src/datalayer
            - Run: /tmp/pulumi/bin/pulumi refresh -C $(pwd)/iac/stacks/src/datalayer --yes --skip-preview --clear-pending-creates --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-refresh"
            - Run: /tmp/pulumi/bin/pulumi preview -C $(pwd)/iac/stacks/src/datalayer  --show-replacement-steps --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-preview"
            - Run: /tmp/pulumi/bin/pulumi up -C $(pwd)/iac/stacks/src/datalayer --yes --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-up"
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/datalayer --json > $(pwd)/_pulumi/datalayer.json
            - Run: cat _pulumi/datalayer.json
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/datalayer --shell > $(pwd)/_pulumi/datalayer.sh
            - Run: cat _pulumi/datalayer.sh
            - Run: /tmp/pulumi/bin/pulumi stack init "$APPLICATION_IMAGE_NAME-panel-http.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/domains/panel/http || true
            - Run: /tmp/pulumi/bin/pulumi stack select "$APPLICATION_IMAGE_NAME-panel-http.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/domains/panel/http || true
            - Run: /tmp/pulumi/bin/pulumi config set aws:skipMetadataApiCheck false -C $(pwd)/iac/stacks/src/domains/panel/http
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.isProd false -C $(pwd)/iac/stacks/src/domains/panel/http
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.features aws -C $(pwd)/iac/stacks/src/domains/panel/http
            - Run: /tmp/pulumi/bin/pulumi config set --path 'frontend:stack.dns.hostnames[0]' "$CI_ENVIRONMENT.$APPLICATION_IMAGE_NAME.cloud.$FRONTEND_HOSTNAME" -C $(pwd)/iac/stacks/src/domains/panel/http
            - Run: /tmp/pulumi/bin/pulumi stack change-secrets-provider $AWS_PROVIDER_KEY -C $(pwd)/iac/stacks/src/domains/panel/http
            - Run: /tmp/pulumi/bin/pulumi refresh -C $(pwd)/iac/stacks/src/domains/panel/http --yes --skip-preview --clear-pending-creates --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-refresh"
            - Run: /tmp/pulumi/bin/pulumi preview -C $(pwd)/iac/stacks/src/domains/panel/http  --show-replacement-steps --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-preview"
            - Run: /tmp/pulumi/bin/pulumi up -C $(pwd)/iac/stacks/src/domains/panel/http --yes --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-up"
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/domains/panel/http --json > $(pwd)/_pulumi/domains_panel_http.json
            - Run: cat _pulumi/domains_panel_http.json
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/domains/panel/http --shell > $(pwd)/_pulumi/domains_panel_http.sh
            - Run: cat _pulumi/domains_panel_http.sh
            - Run: /tmp/pulumi/bin/pulumi stack init "$APPLICATION_IMAGE_NAME-panel-web.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/domains/panel/web || true
            - Run: /tmp/pulumi/bin/pulumi stack select "$APPLICATION_IMAGE_NAME-panel-web.$CI_ENVIRONMENT" -C $(pwd)/iac/stacks/src/domains/panel/web || true
            - Run: /tmp/pulumi/bin/pulumi config set aws:skipMetadataApiCheck false -C $(pwd)/iac/stacks/src/domains/panel/web
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.isProd false -C $(pwd)/iac/stacks/src/domains/panel/web
            - Run: /tmp/pulumi/bin/pulumi config set --path context:stack.environment.features aws -C $(pwd)/iac/stacks/src/domains/panel/web
            - Run: /tmp/pulumi/bin/pulumi config set --path 'frontend:stack.dns.hostnames[0]' "$CI_ENVIRONMENT.$APPLICATION_IMAGE_NAME.cloud.$FRONTEND_HOSTNAME" -C $(pwd)/iac/stacks/src/domains/panel/web
            - Run: /tmp/pulumi/bin/pulumi stack change-secrets-provider $AWS_PROVIDER_KEY -C $(pwd)/iac/stacks/src/domains/panel/web
            - Run: /tmp/pulumi/bin/pulumi refresh -C $(pwd)/iac/stacks/src/domains/panel/web --yes --skip-preview --clear-pending-creates --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-refresh"
            - Run: /tmp/pulumi/bin/pulumi preview -C $(pwd)/iac/stacks/src/domains/panel/web  --show-replacement-steps --json --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-preview"
            - Run: /tmp/pulumi/bin/pulumi up -C $(pwd)/iac/stacks/src/domains/panel/web --yes --suppress-progress --non-interactive --diff --message "${WorkflowSource.BranchName}-${WorkflowSource.CommitId}-up"
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/domains/panel/web --json > $(pwd)/_pulumi/domains_panel_web.json
            - Run: cat _pulumi/domains_panel_web.json
            - Run: /tmp/pulumi/bin/pulumi stack output -C $(pwd)/iac/stacks/src/domains/panel/web --shell > $(pwd)/_pulumi/domains_panel_web.sh
            - Run: cat _pulumi/domains_panel_web.sh
            - Run: du -sh _pulumi
  Deployment:
    DependsOn:
      - Integration
    Actions:
      Push_Image:
        Identifier: aws/build@v1.0.0
        Timeout: 19
        Environment:
          Name: current
        Caching:
          FileCaching:
            a64_npm_global:
              Path: /tmp/npm-global
              RestoreKeys:
                - npminstall
            a64_pnpm_store:
              Path: /tmp/pnpm-store
              RestoreKeys:
                - pnpminstall
        Inputs:
          Sources:
            - WorkflowSource
          Variables:
            - Name: NODEJS_VERSION
              Value: "23"
            - Name: NPM_REGISTRY_PROTOCOL
              Value: https
            - Name: NPM_REGISTRY_HOST
              Value: npm.pkg.github.com
            - Name: NODE_AUTH_TOKEN
              Value: ${Secrets.GITHUB_LEVICAPE_PAT}
            - Name: APPLICATION_IMAGE_NAME
              Value: fourtwo
            - Name: AWS_REGION
              Value: us-west-2
            - Name: CI_ENVIRONMENT
              Value: current
          Artifacts:
            - images
            - pulumi
        Configuration:
          Container:
            Registry: CODECATALYST
            Image: CodeCatalystLinux_Arm64:2024_03
          Steps:
            - Run: npm config set @levicape:registry=$NPM_REGISTRY_PROTOCOL://$NPM_REGISTRY_HOST --location project
            - Run: npm config set //$NPM_REGISTRY_HOST/:_authToken=$NODE_AUTH_TOKEN --location project
            - Run: npm config set prefix=/tmp/npm-global
            - Run: npm exec pnpm config set store-dir /tmp/pnpm-store
            - Run: npm exec n $NODEJS_VERSION
            - Run: npm exec pnpm install --prefer-offline --ignore-scripts
            - Run: ls -la $CATALYST_SOURCE_DIR_images/_images
            - Run: docker load --input $CATALYST_SOURCE_DIR_images/_images/application.tar.gz
            - Run: docker images
            - Run: ls -la $CATALYST_SOURCE_DIR_pulumi/_pulumi
            - Run: cat $CATALYST_SOURCE_DIR_pulumi/_pulumi/codestar.sh
            - Run: source $CATALYST_SOURCE_DIR_pulumi/_pulumi/codestar.sh
            - Run: cat $CATALYST_SOURCE_DIR_pulumi/_pulumi/datalayer.sh
            - Run: source $CATALYST_SOURCE_DIR_pulumi/_pulumi/datalayer.sh
            - Run: cat $CATALYST_SOURCE_DIR_pulumi/_pulumi/domains_panel_http.sh
            - Run: source $CATALYST_SOURCE_DIR_pulumi/_pulumi/domains_panel_http.sh
            - Run: cat $CATALYST_SOURCE_DIR_pulumi/_pulumi/domains_panel_web.sh
            - Run: source $CATALYST_SOURCE_DIR_pulumi/_pulumi/domains_panel_web.sh
            - Run: env
            - Run: |-
                NODE_NO_WARNINGS=1 node -e '(function(){let outputs=JSON.parse(process.env["fourtwo_codestar_ecr"]??"");let envs={"_FOURTWO_CODE_REPOSITORY_URL":outputs.repository.url};Object.entries(envs).forEach(([key,value])=>{process.stdout.write(`export ${key}="${value}"
                `)})})()' > .ci-env
            - Run: cat .ci-env
            - Run: source .ci-env
            - Run: export _AWS_ACCOUNT_ID=$(aws sts get-caller-identity --query Account --output text)
            - Run: aws ecr get-login-password --region $AWS_REGION | docker login --username AWS --password-stdin $_FOURTWO_CODE_REPOSITORY_URL
            - Run: echo $APPLICATION_IMAGE_NAME
            - Run: echo git-${WorkflowSource.CommitId}
            - Run: echo "Tagging $_FOURTWO_CODE_REPOSITORY_URL:git-${WorkflowSource.CommitId}"
            - Run: docker tag $APPLICATION_IMAGE_NAME:latest $_FOURTWO_CODE_REPOSITORY_URL:git-${WorkflowSource.CommitId}
            - Run: docker push $_FOURTWO_CODE_REPOSITORY_URL:git-${WorkflowSource.CommitId}
            - Run: echo $CI_ENVIRONMENT
            - Run: echo "Tagging $_FOURTWO_CODE_REPOSITORY_URL:$CI_ENVIRONMENT"
            - Run: docker tag $APPLICATION_IMAGE_NAME:latest $_FOURTWO_CODE_REPOSITORY_URL:$CI_ENVIRONMENT
            - Run: docker push $_FOURTWO_CODE_REPOSITORY_URL:$CI_ENVIRONMENT
            - Run: npm exec pnpm store prune

########################################
########################################
#**:_$~- {"$$":"head","filename":"[push(b:main)][schedule(0_0_*_*_?_*)]CI.yml","source":"iac/workflows/codecatalyst/push/CI.js"}
#**:_$~- {"$$":"script","generator":"codegen/codecatalyst/GenerateCodeCatalystWorkflow.mts"}
#**:_$~- {"$$":"body","hashed":"96b9a74664d14f1783f86e2c9570a5c23aa0cd10817635b1b5882bc26f5b5c6f"}
#**:_$~- {"$$":"footer","started":"2025-01-25T10:27:05.737Z","now":"2025-01-25T10:27:05.747Z","elapsed":"10ms"}
# END OF GENERATED FILE

