{
	"name": "iac-images-application",
	"targets": {
		"pack:build": {
			"executor": "nx:run-commands",
			"options": {
				"env": {
					"DEFAULT_IMAGE_NAME": "fourtwo",
					"PAKETO_BUILDPACK": "heroku/python,heroku/nodejs,heroku/procfile",
					"PAKETO_BUILDER": "heroku/builder:24"
				},
				"commands": [
					"npx nx run iac-images:cli:pack build $DEFAULT_IMAGE_NAME --buildpack $PAKETO_BUILDPACK --builder $PAKETO_BUILDER --default-process cli  --cache 'type=build;format=bind;source=/cc/cache/docker;type=launch;format=volume;'"
				]
			}
		}
	}
}
