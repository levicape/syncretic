########################################
# THIS FILE WAS AUTOMATICALLY GENERATED, DO NOT MODIFY
########################################
name: "@levicape_fourtwo"
on:
  release:
    types:
      - released
env:
  LEVICAPE_REGISTRY_HOST: npm.pkg.github.com/
  LEVICAPE_REGISTRY: https://npm.pkg.github.com
  LEVICAPE_TOKEN: ${{ secrets.GITHUB_TOKEN }}
jobs:
  publish_fourtwo:
    name: "@levicape/fourtwo: Compile and publish to Github"
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Remove project .npmrc
        run: |-
          if [ -f .npmrc ]; then rm .npmrc; fi;
          if [ -f .npmrc ]; then rm .npmrc; fi;
      - name: Verify registry URL
        continue-on-error: true
        run: |-
          echo "NPM_REGISTRY_URL: ${{ env.LEVICAPE_REGISTRY }}";
          echo "NPM_REGISTRY_HOST: ${{ env.LEVICAPE_REGISTRY_HOST }}";
          curl -v --insecure ${{ env.LEVICAPE_REGISTRY }};
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 22.13.0
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          registry-url: ${{ env.LEVICAPE_REGISTRY }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          scope: "@levicape"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - id: cache-npm
        name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-npm-node-modules-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-build-npm-node-modules-
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV;
      - id: cache-pnpm-store
        name: Cache pnpm files
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-pnpm-store-
      - if: ${{ steps.cache-npm.outputs.cache-hit != 'true' }}
        name: List Dependencies
        continue-on-error: true
        run: pnpm list;
      - name: Install Dependencies
        run: pnpm install;
      - name: "Run package.json: build"
        run: pnpm run build;
      - name: "Run package.json: lint"
        run: pnpm run lint;
      - name: "Run package.json: test"
        run: pnpm run test;
      - name: "Run package.json: prepublishOnly"
        run: pnpm run prepublishOnly;
      - name: Increment version
        env:
          RELEVANT_SHA: ${{ github.event.release.target_commitish || github.sha }}
        run: |-
          export PREID=$RELEVANT_SHA;
          export PREID=${PREID:0:10};
          export ARGS="--git-tag-version=false --commit-hooks=false";
          npm version ${{ github.event.release.tag_name }}-${PREID:-unknown}.${{ github.run_number }} $ARGS --allow-same-version;
      - if: success()
        name: Publish to npm
        continue-on-error: true
        run: pnpm publish --no-git-checks;
  publish_fourtwo-pulumi:
    name: "@levicape/fourtwo-pulumi: Compile and publish to Github"
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    defaults:
      run:
        working-directory: packages/pulumi
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Remove project .npmrc
        run: |-
          if [ -f ../../.npmrc ]; then rm ../../.npmrc; fi;
          if [ -f .npmrc ]; then rm .npmrc; fi;
      - name: Verify registry URL
        continue-on-error: true
        run: |-
          echo "NPM_REGISTRY_URL: ${{ env.LEVICAPE_REGISTRY }}";
          echo "NPM_REGISTRY_HOST: ${{ env.LEVICAPE_REGISTRY_HOST }}";
          curl -v --insecure ${{ env.LEVICAPE_REGISTRY }};
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 22.13.0
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          registry-url: ${{ env.LEVICAPE_REGISTRY }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          scope: "@levicape"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - id: cache-npm
        name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-npm-node-modules-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-build-npm-node-modules-
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV;
      - id: cache-pnpm-store
        name: Cache pnpm files
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-pnpm-store-
      - if: ${{ steps.cache-npm.outputs.cache-hit != 'true' }}
        name: List Dependencies
        continue-on-error: true
        run: pnpm list;
      - name: Install Dependencies
        run: pnpm install;
      - name: "Run package.json: build"
        run: pnpm run build;
      - name: "Run package.json: lint"
        run: pnpm run lint;
      - name: "Run package.json: test"
        run: pnpm run test;
      - name: "Run package.json: prepublishOnly"
        run: pnpm run prepublishOnly;
      - name: Increment version
        env:
          RELEVANT_SHA: ${{ github.event.release.target_commitish || github.sha }}
        run: |-
          export PREID=$RELEVANT_SHA;
          export PREID=${PREID:0:10};
          export ARGS="--git-tag-version=false --commit-hooks=false";
          npm version ${{ github.event.release.tag_name }}-${PREID:-unknown}.${{ github.run_number }} $ARGS --allow-same-version;
      - if: success()
        name: Publish to npm
        continue-on-error: true
        run: pnpm publish --no-git-checks;
  publish_fourtwo-builders:
    name: "@levicape/fourtwo-builders: Compile and publish to Github"
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    defaults:
      run:
        working-directory: packages/builders
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Remove project .npmrc
        run: |-
          if [ -f ../../.npmrc ]; then rm ../../.npmrc; fi;
          if [ -f .npmrc ]; then rm .npmrc; fi;
      - name: Verify registry URL
        continue-on-error: true
        run: |-
          echo "NPM_REGISTRY_URL: ${{ env.LEVICAPE_REGISTRY }}";
          echo "NPM_REGISTRY_HOST: ${{ env.LEVICAPE_REGISTRY_HOST }}";
          curl -v --insecure ${{ env.LEVICAPE_REGISTRY }};
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 22.13.0
        uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          registry-url: ${{ env.LEVICAPE_REGISTRY }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          scope: "@levicape"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - id: cache-npm
        name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-npm-node-modules-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-build-npm-node-modules-
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV;
      - id: cache-pnpm-store
        name: Cache pnpm files
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-pnpm-store-
      - if: ${{ steps.cache-npm.outputs.cache-hit != 'true' }}
        name: List Dependencies
        continue-on-error: true
        run: pnpm list;
      - name: Install Dependencies
        run: pnpm install;
      - name: "Run package.json: build"
        run: pnpm run build;
      - name: "Run package.json: lint"
        run: pnpm run lint;
      - name: "Run package.json: test"
        run: pnpm run test;
      - name: "Run package.json: prepublishOnly"
        run: pnpm run prepublishOnly;
      - name: Increment version
        env:
          RELEVANT_SHA: ${{ github.event.release.target_commitish || github.sha }}
        run: |-
          export PREID=$RELEVANT_SHA;
          export PREID=${PREID:0:10};
          export ARGS="--git-tag-version=false --commit-hooks=false";
          npm version ${{ github.event.release.tag_name }}-${PREID:-unknown}.${{ github.run_number }} $ARGS --allow-same-version;
      - if: success()
        name: Publish to npm
        continue-on-error: true
        run: pnpm publish --no-git-checks;

########################################
########################################
#**:_$~- {"$$":"head","filename":"[release(released)] - Publish - [@levicape_fourtwo].yml","source":"iac/workflows/github/release/Publish.js"}
#**:_$~- {"$$":"script","generator":"codegen/github/GenerateGithubWorkflow.mts"}
#**:_$~- {"$$":"body","hashed":"f1338a2debc09eb4f455bd1e6e9b8dadacdbdd5708cdae78642263b34978afc0"}
#**:_$~- {"$$":"footer","started":"2025-03-17T05:39:37.612Z","now":"2025-03-17T05:39:37.621Z","elapsed":"9ms"}
# END OF GENERATED FILE

