########################################
# THIS FILE WAS AUTOMATICALLY GENERATED, DO NOT MODIFY
########################################
name: "on Push: Test Package"
on:
  push:
    branches:
      - main
env:
  NPM_REGISTRY_PROTOCOL: https
  NPM_REGISTRY_HOST: npm.pkg.github.com
jobs:
  build:
    name: Compile, Lint and Test all workspace packages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 22.12.0
        uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
          registry-url: ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          scope: "@levicape"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - id: cache-npm
        name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-npm-node-modules-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-build-npm-node-modules-
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV;
      - id: cache-pnpm-store
        name: Cache pnpm files
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-pnpm-store-
      - if: ${{ steps.cache-npm.outputs.cache-hit != 'true' }}
        name: List Dependencies
        continue-on-error: true
        run: pnpm list;
      - name: Install Dependencies
        run: pnpm install;
      - name: Compile
        run: pnpx nx run-many -t compile --parallel=1 --verbose --no-cloud;
      - name: Lint
        run: pnpx nx run-many -t lint --parallel=1 --verbose --no-cloud;
      - name: Test
        run: pnpx nx run-many -t test --parallel=1 --verbose --no-cloud;

########################################
########################################
#**:_$~- {"$$":"head","filename":"[push(b:main)]GithubMainPush.yml","source":"iac/workflows/github/push/GithubMainPush.js"}
#**:_$~- {"$$":"script","generator":"codegen/github/GenerateGithubWorkflow.mts"}
#**:_$~- {"$$":"body","hashed":"580670b8bd6aeda5a37edf6f8a1ee780ab7f749f57fdf55a44b5709dfe74e35b"}
#**:_$~- {"$$":"footer","started":"2024-12-20T13:05:22.435Z","now":"2024-12-20T13:05:22.441Z","elapsed":"6ms"}
# END OF GENERATED FILE

