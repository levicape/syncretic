########################################
# THIS FILE WAS AUTOMATICALLY GENERATED, DO NOT MODIFY
########################################
name: "on Release: [released] Publish packages to Github"
on:
  release:
    types:
      - released
env:
  NPM_REGISTRY_PROTOCOL: https
  NPM_REGISTRY_HOST: npm.pkg.github.com
jobs:
  publish_fourtwo:
    name: "@levicape/fourtwo: Compile and publish to Github"
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Verify registry URL
        continue-on-error: true
        run: |-
          echo "NPM_REGISTRY_URL: ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }}";
          curl -v --insecure ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }};
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 22.12.0
        uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
          registry-url: ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          scope: "@levicape"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - id: cache-npm
        name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-npm-node-modules-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-build-npm-node-modules-
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV;
      - id: cache-pnpm-store
        name: Cache pnpm files
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-pnpm-store-
      - if: ${{ steps.cache-npm.outputs.cache-hit != 'true' }}
        name: List Dependencies
        continue-on-error: true
        run: pnpm list;
      - name: Install Dependencies
        run: pnpm install;
      - name: "Run package.json: compile"
        run: pnpm run compile;
      - name: "Run package.json: lint"
        run: pnpm run lint;
      - name: "Run package.json: test"
        run: pnpm run test;
      - name: "Run package.json: prepublish"
        run: pnpm run prepublish;
      - name: Increment version
        env:
          RELEVANT_SHA: ${{ github.event.release.target_commitish || github.sha }}
        run: |-
          export PREID=$RELEVANT_SHA;
          export PREID=${PREID:0:10};
          export ARGS="--git-tag-version=false --commit-hooks=false";
          npm version ${{ github.event.release.tag_name }}-$PREID.${{ github.run_number }} $ARGS --allow-same-version;
      - if: success()
        name: Publish to npm
        continue-on-error: true
        run: pnpm publish --no-git-checks;
  publish_fourtwo-react:
    name: "@levicape/fourtwo-react: Compile and publish to Github"
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - name: Verify registry URL
        continue-on-error: true
        run: |-
          echo "NPM_REGISTRY_URL: ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }}";
          curl -v --insecure ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }};
      - name: Checkout
        uses: actions/checkout@v2
      - name: Setup pnpm
        uses: pnpm/action-setup@v4
      - name: Setup Node 22.12.0
        uses: actions/setup-node@v4
        with:
          node-version: 22.12.0
          registry-url: ${{ env.NPM_REGISTRY_PROTOCOL }}://${{ env.NPM_REGISTRY_HOST }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml
          scope: "@levicape"
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - id: cache-npm
        name: Cache node modules
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-build-npm-node-modules-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-build-npm-node-modules-
      - name: Get pnpm store directory
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV;
      - id: cache-pnpm-store
        name: Cache pnpm files
        uses: actions/cache@v3
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.json') }}
          restore-keys: ${{ runner.os }}-pnpm-store-
      - if: ${{ steps.cache-npm.outputs.cache-hit != 'true' }}
        name: List Dependencies
        continue-on-error: true
        run: pnpm list;
      - name: Install Dependencies
        run: pnpm install;
      - name: Navigate to package directory
        run: cd packages/react;
      - name: "Run package.json: compile"
        run: pnpm run compile;
      - name: "Run package.json: lint"
        run: pnpm run lint;
      - name: "Run package.json: test"
        run: pnpm run test;
      - name: "Run package.json: prepublish"
        run: pnpm run prepublish;
      - name: Increment version
        env:
          RELEVANT_SHA: ${{ github.event.release.target_commitish || github.sha }}
        run: |-
          export PREID=$RELEVANT_SHA;
          export PREID=${PREID:0:10};
          export ARGS="--git-tag-version=false --commit-hooks=false";
          npm version ${{ github.event.release.tag_name }}-$PREID.${{ github.run_number }} $ARGS --allow-same-version;
      - if: success()
        name: Publish to npm
        continue-on-error: true
        run: pnpm publish --no-git-checks;

########################################
########################################
#**:_$~- {"$$":"head","filename":"[release(released)]GithubReleaseReleased.yml","source":"iac/workflows/github/release/GithubReleaseReleased.js"}
#**:_$~- {"$$":"script","generator":"codegen/github/GenerateGithubWorkflow.mts"}
#**:_$~- {"$$":"body","hashed":"7929d5cd12cb37232f9d48426ca426f8cbb08ade22cb627b831f4efaf1c17978"}
#**:_$~- {"$$":"footer","started":"2024-12-20T05:27:43.932Z","now":"2024-12-20T05:27:43.946Z","elapsed":"14ms"}
# END OF GENERATED FILE

